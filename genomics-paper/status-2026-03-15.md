# Status – 15 Mar 2026

## Completed today
- Drafted data acquisition + conversion scripts:
  - `scripts/download_replogle.py` to pull the Replogle Perturb-seq atlas from the Broad portal and package it as AnnData with split metadata hooks.
  - `scripts/convert_mixology.py` to convert the Mixology combinatorial dataset (GSE121981) into `.h5ad` with combo labels ready for held-out-pair evaluation.
- Documented processing plan (`data-pipeline.md`) and baseline plan were already in place; scripts operationalize step 1.

## In progress
- Need to parameterize download script with actual portal file IDs once confirmed.
- Next up: ECCITE-seq conversion script + QC notebook template.
